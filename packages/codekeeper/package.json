{
  "name": "codekeeper",
  "version": "0.2.0",
  "private": true,
  "description": "Complete CLI, TUI, runtime, and workflow release package for Codekeeper",
  "type": "module",
  "bin": {
    "codekeeper": "bin/codekeeper.mjs",
    "codekeeper-verify-package": "bin/verify-package.mjs"
  },
  "files": [
    "assets/",
    "bin/",
    "release/",
    "runtime/",
    "src/",
    "LICENSE",
    "README.md",
    "npm-shrinkwrap.json"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prepare:runtime-test": "npm --prefix runtime-package ci --ignore-scripts --no-audit --no-fund",
    "test:unit": "node --test test/*.test.mjs audit/*.test.mjs",
    "test": "npm run prepare:runtime-test && npm run test:unit",
    "check": "npm run prepare:runtime-test && node ../../scripts/sync-policy-validator.mjs --check && bash -c 'for file in bin/*.mjs src/*.mjs test/*.mjs audit/*.mjs; do node --check \"$file\"; done' && npm run test:unit"
  },
  "keywords": [
    "github",
    "automation",
    "codekeeper"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "ink": "7.1.1",
    "react": "19.2.8"
  }
}
