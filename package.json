{
  "name": "rivet-repository",
  "private": true,
  "type": "module",
  "packageManager": "npm@12.0.2",
  "scripts": {
    "rivet:check": "npm --prefix packages/rivet ci --ignore-scripts --no-audit --no-fund && npm --prefix packages/rivet run check",
    "coverage": "npm --prefix tools/codekeeper run coverage",
    "governance:check": "node scripts/repository-governance.mjs --validate && node --test scripts/repository-governance.test.mjs",
    "architecture:check": "node scripts/check-module-boundaries.mjs && node scripts/check-local-import-cycles.mjs && node --test scripts/check-module-boundaries.test.mjs scripts/check-local-import-cycles.test.mjs",
    "lint": "eslint .",
    "format": "prettier --write package.json eslint.config.mjs tools/codekeeper/src/lib/commands.mjs",
    "format:check": "prettier --check package.json eslint.config.mjs tools/codekeeper/src/lib/commands.mjs",
    "check": "npm run rivet:check && npm run lint && npm run format:check && npm run governance:check && npm run architecture:check && bash scripts/release-source.sh --verify-worktree"
  },
  "devDependencies": {
    "acorn": "8.18.0",
    "eslint": "10.8.1",
    "prettier": "3.9.6"
  }
}
